# ================================================
# Stream Plus - Variables de Entorno
# ================================================
# Copia este archivo a .env y personaliza los valores
# Copy this file to .env and configure variables

# ================================================
# CONFIGURACIÓN DE DISPATCHARR (REQUERIDO)
# Dispatcharr API Configuration (REQUIRED)
# ================================================
DISPATCHARR_API_URL=http://localhost:8080
DISPATCHARR_API_USER=admin
DISPATCHARR_API_PASSWORD=changeme

# ================================================
# CONFIGURACIÓN DE FLASK (OPCIONAL)
# Flask Configuration (OPTIONAL)
# ================================================
PORT=5000
FLASK_DEBUG=False
SECRET_KEY=dev-secret-key-change-in-production

# ================================================
# CONFIGURACIÓN DOCKER (OPCIONAL)
# Docker Configuration (OPTIONAL)
# ================================================
# Zona horaria del contenedor / Container timezone
TZ=Europe/Madrid

# UID y GID del usuario (para permisos de archivos)
# User UID and GID (for file permissions)
# Linux/Mac: usa $(id -u) y $(id -g)
# Windows: normalmente 1000
USER_UID=1000
USER_GID=1000

# ================================================
# LOGGING (OPCIONAL / OPTIONAL)
# ================================================
LOG_LEVEL=INFO
LOG_FILE=stream_plus.log

# ================================================
# STREAM TESTING CONFIGURATION (OPCIONAL / OPTIONAL)
# ================================================
# Duration in seconds to analyze streams during testing (default: 10)
STREAM_TEST_DURATION=10
# Additional timeout buffer in seconds for stream testing operations (default: 30)
STREAM_TEST_TIMEOUT_BUFFER=30
# Delay in seconds between consecutive stream tests to avoid provider detection (default: 3)
STREAM_TEST_DELAY=3
# User-Agent string to use for stream testing with ffmpeg/ffprobe (default: Chrome 132 user agent)
STREAM_TEST_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/132.0.0.0 Safari/537.3

# ================================================
# PERFORMANCE CONFIGURATION (OPCIONAL / OPTIONAL)
# ================================================
# Cache TTL in seconds for channel groups and statistics (default: 300 = 5 minutes)
# Lower values provide more up-to-date data but increase API calls
# Higher values improve performance but may show stale data
CACHE_TTL=300
